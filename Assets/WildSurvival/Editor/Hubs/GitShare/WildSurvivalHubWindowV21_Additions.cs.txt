using UnityEditor;
using UnityEngine;

namespace WildSurvival.Editor
{
    public partial class WildSurvivalHubWindowV21
    {
        // This file assumes the main Hub class exists and is partial; if not, it's safe to ignore.
        // Adds extra links to new tools.
        void OnGUI_Additions()
        {
            DrawSection("Play", () =>
            {
                LinkButton("Playtest: Set Playtest Scene & Play", "WildSurvival/Tools (V2)/Playtest: Set Playtest Scene & Play");
            });

            DrawSection("CSV / Data", () =>
            {
                LinkButton("CSV: Create Templates", "WildSurvival/Tools (V2)/CSV/Create Templates");
                LinkButton("CSV: Open Folder", "WildSurvival/Tools (V2)/CSV/Open Folder");
            });

            DrawSection("Bug Reports", () =>
            {
                LinkButton("Export Bug Report Bundle", "WildSurvival/Tools (V2.1)/Export Bug Report Bundle");
            });
        }
    }
}
